#include <stdio.h>

/**
 * main - prints the first 98 fibonacci numbers
 *
 * Return: nothing
 */
int main(void)
{
	int i;
	long int a = 1;
	long int b = 2;

	for (i = 0; i < 98; i++)
	{
		if (i == 0)
			printf("%ld", j);
		else if (i == 1)
			printf(", %ld", k);
		else
		{
			t = k;
			k += j;
			printf(", %ld", k);
			j = t;
		}
	}

	printf("\n");

	return (0);
}
